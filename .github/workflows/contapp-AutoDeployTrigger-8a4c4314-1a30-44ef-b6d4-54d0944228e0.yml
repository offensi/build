name: Trigger auto deployment for contapp

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/contapp-AutoDeployTrigger-8a4c4314-1a30-44ef-b6d4-54d0944228e0.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CONTAPP_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAPP_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAPP_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: recon.offensi.com
          registryUsername: ${{ secrets.CONTAPP_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAPP_REGISTRY_PASSWORD }}
          containerAppName: contapp
          resourceGroup: backdoorgroup
          imageToBuild: recon.offensi.com/contapp:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


